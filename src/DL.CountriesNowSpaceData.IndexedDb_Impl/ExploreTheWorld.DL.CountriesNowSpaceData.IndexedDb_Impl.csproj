<Project Sdk="Microsoft.NET.Sdk.Razor">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AssemblyName>JBC.ExploreTheWorld.DL.CountriesNowSpaceData.IndexedDb_Impl</AssemblyName>
    <RootNamespace>JBC.ExploreTheWorld.DL.CountriesNowSpaceData.IndexedDb_Impl</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- References DL (entity types + repo interface) but NOT DL.CountriesNowSpaceData — no EF Core DbContext needed. -->
    <ProjectReference Include="..\DL\ExploreTheWorld.DL.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Components.Web" Version="10.0.10" />
    <PackageReference Include="Microsoft.JSInterop" Version="10.0.10" />
  </ItemGroup>

</Project>
